user_name = "hyemin"
user_age = 20
if user_age < 20:
    age_type = "minors"
elif user_age >= 20:
    age_type = "adults"
else :
    print("잘못 입력하셨습니다.")

count = 0
while count < 3:
    print(count)
    count += 1

user = [1]
user.append(4)
print(user)

def add(name, income):
    if name == true :
        return 1

class Account:
    def __init__(self, name, income):
        self.name = name
        self.balance = income
    
    def deposit(self, amount):
        self.balance += amount